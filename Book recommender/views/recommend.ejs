<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Recommendations</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Basic CSS styles for improved UI */
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #f3f4f6;
        }

        .container {
            text-align: center;
            width: 100%;
            max-width: 600px;
            padding: 20px;
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
        }

        .form-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
        }

        label, input, button {
            display: block;
            width: 100%;
            margin-bottom: 10px;
        }

        input[type="text"] {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            padding: 10px;
            font-size: 16px;
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
        }

        .message {
            color: #666;
            font-size: 16px;
            margin-top: 10px;
        }

        .recommendations {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: center;
        }

        .book-card {
            background-color: #ffffff;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 150px;
            text-align: center;
            transition: transform 0.2s;
        }

        .book-card:hover {
            transform: translateY(-5px);
        }

        .book-card img {
            width: 100%;
            height: auto;
            border-radius: 4px;
            margin-bottom: 10px;
        }

        .book-title {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .book-author {
            font-size: 14px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Book Recommendations</h1>
        
        <div class="form-container">
            <form action="/recommend_books" method="POST">
                <label for="user_input">Enter a Book Title:</label>
                <input type="text" id="user_input" name="user_input" required placeholder="e.g., Harry Potter">
                <button type="submit">Get Recommendations</button>
            </form>
        </div>

        <% if (message) { %>
            <p class="message"><%= message %></p>
        <% } else { %>
            <h2>Recommended Books</h2>
            <div class="recommendations">
                <% data.forEach(item => { %>
                    <div class="book-card">
                        <img src="<%= item[2] %>" alt="<%= item[0] %>">
                        <p class="book-title"><%= item[0] %></p>
                        <p class="book-author">by <%= item[1] %></p>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</body>
</html>
